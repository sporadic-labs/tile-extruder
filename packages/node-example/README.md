# Node Example

A minimal demo of using tile-extruder in a node app.

## Running

Install pnpm and then run the following commands from the root of the repo:
- `pnpm install`
- `pnpm --filter node-example start`